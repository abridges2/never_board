<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Never Board" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Bootstrap 5.3.3 CSS CDN %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "active_admin", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-dark py-4 border-bottom shadow-sm">
      <div class="container-fluid">
        <!-- Navbar brand -->
        <a href="https://fontmeme.com/resize-images/"><img src="https://fontmeme.com/permalink/250411/babd8beff0b750d37e6ab75168aec56e.png" alt="resize-images" border="0"></a>


        <!-- Toggle for mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
          aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapsible content -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <!-- Left links -->
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item ">
              <a class="nav-link fs-4" href="#">About</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fs-4" href="#">Contact</a>
            </li>
            <li class="nav-item">
              <%= link_to "Products", products_path, class: "nav-link fs-4"%>
            </li>
          </ul>

          <!-- Cart and Search (right side) -->
          <div class="d-flex align-items-center gap-0">

            <a href="#" class="btn btn-outline-light me-3 cart-btn">
              <i class="bi bi-cart3"></i>
            </a>

            <%= form_with(url: search_products_path, method: "get", local: true, html: { class: "d-flex gap-2 form-inline"}) do %>
              <%= text_field_tag(:query, nil, class: "form-control rounded", placeholder: "Search product") %>
              <select class="form-select" name="category_id" id="form-select">
                <option value="">All categories</option>
                <% Category.all.each do |category| %>
                  <option value="<%= category.id %>"><%= category.name %></option>
                <% end %>
              </select>
              <% submit_tag("Search") %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    <!-- Navbar -->

    <%= yield %>

    <%# Bootstrap 5.3.3 JS Bundle CDN %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
